<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ostopd.imageviewpager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LauncherActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SimpleSampleActivity"
            android:label="@string/title_activity_simple_sample"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewPagerActivity"
            android:label="@string/title_activity_view_pager"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RotationSampleActivity"
            android:label="@string/title_activity_rotation_sample"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PicassoSampleActivity"
            android:label="@string/title_activity_picasso_sample"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>